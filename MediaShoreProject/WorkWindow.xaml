<Window x:Class="MediaShoreProject.WorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaShoreProject"
        mc:Ignorable="d"
        Title="MediaStore" Height="678" Width="974"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
            <StackPanel Grid.Row="0" HorizontalAlignment="Left" Orientation="Horizontal">
                <Button x:Name="btnDisconnect" Content="Выйти" Background="red" Foreground="white" Click="btnDisconnect_Click" Width="100" Height="51" FontSize="22"/>
            <StackPanel Orientation="Horizontal" Margin="50,0,0,0">
                <TextBlock VerticalAlignment="Center" Text="Поиск: "/>
                <TextBox x:Name="tbSearch" Width="300" Height="50" VerticalContentAlignment="Center" TextChanged="tbSearch_TextChanged" HorizontalAlignment="Left" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Тип диска: " VerticalAlignment="Center" Margin="10,0,0,0"/>
                <ComboBox x:Name="cbDiscType" Height="50" VerticalContentAlignment="Center"  SelectionChanged="cbDiscType_SelectionChanged"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Сортировка: " VerticalAlignment="Center" Margin="10,0,0,0"/>
                <StackPanel VerticalAlignment="Center">
                    <RadioButton x:Name="rbAB" Content="По алфавиту" Checked="rbAB_Checked"/>
                    <RadioButton x:Name="rbBA" Content="Против алфавита" Checked="rbBA_Checked"/>
                    <RadioButton x:Name="rbDEF" Content="Не сортировать" Checked="rbDEF_Checked" IsChecked="True"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>


        <ListBox Grid.Row="1" x:Name="lbDisc" ScrollViewer.VerticalScrollBarVisibility="Auto" MouseDoubleClick="lbDisc_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="400"/>
                                <ColumnDefinition Width="300"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="{Binding discCoverPath}" Margin="0,0,10,0" Width="100" Height="100"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding discName}" FontSize="22"/>
                                <TextBlock Text="{Binding Authors.authorFullName}"/> <!-- НЕ ЗАБУДЬ ТУТА // не забыл!-->
                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                    <TextBlock Text="{Binding ReleaseDate}" FontStyle="Italic" FontSize="10"/>
                                    <TextBlock Text=" / "/>
                                    <TextBlock Text="{Binding DiscGenres.genreName}" FontStyle="Italic" FontSize="10"/>
                                    
                                </StackPanel>
                            <TextBlock Text="{Binding discDescription}" FontSize="18" TextWrapping="WrapWithOverflow"/> 
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <!--<TextBlock Text="{Binding quantity}" FontSize="26"/>-->
                            <TextBlock Text="{Binding price}"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="-" Width="30" Height="30" Click="Button_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding quantity}" Value="0">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <TextBlock Text="{Binding quantity}" Margin="15,0" FontSize="18" 
                           VerticalAlignment="Center" MinWidth="25" TextAlignment="Center"/>

                                <!-- Кнопка Плюс -->
                                <Button Content="+" Width="30" Height="30" Click="Button_Click_1"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>


        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock x:Name="textBlockUserRole" Text="ROLE:"/>
                <TextBlock Margin="5,0,0,0" x:Name="textBlockUserFIO" Text="NICKNAME"/>
                <TextBlock Margin="5,0,0,0" x:Name="textBlockBalance" Text="(BALANCE)"/>
                <Button x:Name="btnCart" Content="Корзина (0)" Margin="20,0,0,0" Click="btnCart_Click" Width="120"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="50,0,0,0">
                <Button x:Name="btnAdd" Content="(+) Добавить новый" Click="btnAdd_Click" />
                <Button x:Name="btnEdit" Content="(~) Редактировать" Margin="10,0,10,0" Click="btnEdit_Click" />
                <Button x:Name="btnDelete" Content="(x) Удалить" Click="btnDelete_Click" />
                <Button x:Name="btnAdmin" Content="Админпанель" Margin="30,0,0,0" Width="83" Background="Orange" Foreground="Black" Click="btnAdmin_Click" />
                <Button Margin="5,0,0,0" x:Name="btnRefresh" Content="Обновить" Background="Lime" Click="btnRefresh_Click"/>
            </StackPanel>
        </StackPanel>
        
        

    </Grid>
</Window>
